package dk.group42;

/**
 * Placeholder. We use resolution (see {@link Resolution}) as the
 * entailment engine, so a truth-table satisfiability check is not
 * needed for the mandatory parts of the assignment.
 *
 * <p>If you want a cross-check while developing, a small truth-table
 * evaluator (one row per assignment of the propositional symbols)
 * would fit naturally here and could be compared against
 * {@code Resolution.isUnsatisfiable} on generated CNF.
 */
public class TruthTable {
}
